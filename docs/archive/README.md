# 보존용 옛 문서

**여기 있는 문서는 현재 구현을 설명하지 않는다.** 지금 구현은
[`docs/PIPELINE.md`](../PIPELINE.md) 를 본다.

버리지 않고 남긴 이유는, **당시에 왜 그렇게 판단했는지**가 기록되어 있어서다.
"왜 3시간 블록이었나", "왜 익산 AWS 를 기본값으로 삼았나" 같은 질문에
이 문서들이 답한다. 결론이 바뀐 것이지 그때의 추론이 엉터리였던 게 아니다.

| 문서 | 시점 | 내용 |
| --- | --- | --- |
| `2026-08-10_구현 파이프라인 설명서.md` | v5 종료 | 데이터 폴더 구조 · 단계별 설명 · 검증 5라운드 결론 |
| `2026-08-10_악취 분뇨 프로젝트 구현.md` | v5 종료 | 팀 구현 매뉴얼 · 공통 규약 · S0~S8 상세 |

두 문서 모두 첫머리에 **현행과 무엇이 다른지 대조표**가 붙어 있다.

## 읽을 때 주의

```
   성능 수치를 인용하지 말 것
   PR-AUC 0.305 · 주간적중 0.467 · 양성률 13.2% 는 전부 3시간 블록 격자 기준이다.
   현행은 1시간 격자라 분모가 달라 직접 비교가 성립하지 않는다.

   폴더 이름이 다름
   etl / ops / scoring  →  preprocess / serving / advisor 로 바뀌었다.
   변경 이력은 demo/docs/RENAME_2026-08-15.md 에 있다.
```

검증 라운드 원본 기록은 `demo/out/validation_report*.md` 에 그대로 있다.
