# 발표 준비 — 익산시 축산 악취 민원 저감 의사결정 시스템

> 작성 2026-08-23 · 기준 커밋 `0a48e26` (main)
> 이 문서의 모든 수치는 **2026-08-23에 실제로 `run_train.py` → `run_serve.py` → `run_check.py` 를
> 완주시켜 얻은 값**이다. `demo/README.md` 의 성능표는 구 데이터셋 기준이라 값이 다르다.
> 문서와 파일이 다르면 `demo/out/training_results.json` 이 옳다.

**한 줄 요약** — "냄새 나는 작업을 언제 하면 민원이 덜 날까"를 시각 단위로 답한다.
농가가 액비를 뿌리거나 분뇨를 반출할 때, 앞으로 3~7일 중 어느 시각이 가장 안전한지 순위를 매긴다.

---

## 0. 발표 전 반드시 알고 갈 것

| 항목 | 상태 |
| --- | --- |
| A/B (ML·플룸) 파이프라인 | ✅ 지금 그대로 완주. 회귀 점검 39항목 전부 통과 |
| 기상청 API | ✅ 실 API 연결 (단기 66시점 + 중기 D+4~7) |
| C (RAG) | ✅ **Supabase pgvector 이관 완료** — 문서 12개 → 청크 **567개** → `rag` 스키마 5.4MB |
| D (Agent) | ✅ 단위테스트 11개 통과 · Streamlit UI 렌더링 확인 · Supabase+pgvector+Gemini 통합 검증 완료 |
| **법령 하드필터** | ❌ **미구현** — 아래 참조 |

### 질문받으면 곤란한 3가지 (선제적으로 말하는 편이 낫다)

1. **법령 하드필터가 없다.** 파이프라인 설계상 `법령 → ML → 플룸` 순서인데, 1단계인
   `rules/spread_rules.py`(강우·결빙·부숙도·질소상한)가 코드에 존재하지 않는다.
   **이것은 RAG와 다른 층이다** — 절대규칙 3(판단=코드 / 설명=RAG)에 따라 RAG는 조문을
   *찾아 보여줄* 뿐이고, 시각 후보를 실제로 *잘라내는* 코드는 `advisor/recommend.py` 에 없다.
   `run_serve.py` 17행의 섹션 제목에 `"법령(미구현)"` 이라고만 적혀 있다.
   → **지금 나오는 추천은 법적 제약을 전혀 안 거른 결과다.**
2. **grouped CV가 무의미하다.** `model/train_model.py` 의 `grouped_cv()` 는 매 학습마다
   `PR-AUC 1.0000` 을 찍는데, `region` 컬럼이 양성 행에만 있어서 테스트셋이 전부 양성이 되기 때문이다
   (양성률 1.0 → PR-AUC는 정의상 1.0). 즉 "지역을 외웠는지 일반 규칙을 배웠는지"는 **아직 검증되지 않았다.**
   같은 파일의 `grouped_cv_regional()` docstring에 이 버그가 이미 적혀 있다.
3. **RAGAS `answer_relevancy` 가 전부 `nan`이다.** 4개 지표 중 하나가 측정 실패 상태다.

> 📌 **문서 정정**: README 는 "저장소가 공개 상태"라며 API 키 재발급을 최우선으로 적었지만,
> `gh repo view` 확인 결과 **PRIVATE** 이다. 발표에서 "공개 저장소에 키가 노출됐다"고
> 말하면 사실과 다르다. 재발급은 여전히 필요하되 긴급도는 낮다.

---

## 1. 전체 아키텍처 — 어떤 파일이 실제로 도는가

**한 문장** — A가 모델을 만들고 → B가 매일 위험도를 계산해 Supabase에 넣고 →
C가 법령을 벡터로 만들어 같은 DB에 넣고 → **D는 그 DB만 읽는다.**

핵심은 **Supabase가 유일한 접점**이라는 것이다. D는 모델 파일도 PDF도 직접 건드리지 않는다.

```
                                                       ┌──────────────────────┐
  원본 데이터      [A] 학습         [B] 매일 서빙        │  SUPABASE            │     [D] 에이전트
                                                       │                      │
  민원 16,113 ──▶ run_train.py ──▶ run_serve.py   ──▶ │  public 스키마       │ ──▶ work_guide.py
  기상 66,882     preprocess/      daily_scoring       │   risk_hourly        │     dashboard.py
  농가  2,677     model/           기상청 예보 API      │   farm_config        │
                  ↓                      │             │   forecast_hourly ✗  │     ① 도구 4개 호출
                  model_*.pkl            │             │                      │     ② 6시간 창 계산
                  (D는 접근 안 함)        │             │  rag 스키마          │     ③ Top3 확정
                                         │             │   chunks 567         │     ④ Gemini 설명
  PDF 12개  ──▶  [C] RAG      ───────────────────────▶ │   query_cache 5      │
  법령6·매뉴얼6   _4b_migrate_to_pg.py                  └──────────────────────┘
                  (_1→_2→_3 재사용)
                                         │
                                         ▼  같은 실행의 둘째 갈래
                               advisor/recommend.py
                               플룸 · 등급 · 조언
                                         │
                                         ▼
                                    콘솔 print()  ──✗──▶ 저장 안 됨 · D가 읽을 방법 없음
```

### 1-1. 파트별 연결 상태

| 파트 | 상태 | 무엇을 통해 |
| --- | --- | --- |
| **B** 서빙 | ✅ 완전 연결 | `public.risk_hourly` · `farm_config` 를 Supabase에서 직접 조회 |
| **C** RAG | ✅ 완전 연결 | `rag.chunks` 567청크, pgvector 코사인 검색 |
| **A** 모델 | 🟡 간접 연결 | `.pkl` 을 열지 않음. **A→B→D 단방향** |
| 플룸 | ❌ 미연결 | `advisor/recommend.py` 가 콘솔로만 출력 |
| 측정소 | ❌ 미연결 | 정보공개청구 자료 대기 (탭 ①은 빈 화면) |

### 1-2. `run_serve.py` 는 서로 다른 두 가지 일을 한다

이게 혼동의 핵심이다. 명령 하나가 갈래 둘로 나뉜다.

| 갈래 | 파일 | 하는 일 | 결과가 어디로 |
| --- | --- | --- | --- |
| **첫째** | `serving/daily_scoring.py` | 예보 API → 피처 → 모델 → 확률 → 등급 | ✅ **Supabase 적재** — D가 읽음 |
| **둘째** | `advisor/recommend.py` | 법령필터(미구현) → ML랭킹 → **플룸 그룹선택** → 등급 → 조언 | ❌ **콘솔 print** — 저장 안 됨 |

`advisor/recommend.py` 는 DB를 **읽기만** 하고 `INSERT`/`UPDATE` 가 하나도 없다.
반환값을 `run_serve.py` 가 화면에 찍고 끝이다.

> **플룸에 대한 정확한 표현**
> 플룸의 역할은 "근거리/원거리 두 그룹 중 어느 쪽을 볼지 고르는 것"이 맞다.
> 다만 그 선택은 `advisor/recommend.py` 안에서만 일어나고, 결과가 저장되지 않아
> **D는 그 선택을 받지 못한다.** D는 대신 두 그룹 중 **높은 확률을 택한다**(보수적 max,
> `legacy_provider.py:117`). 설계 의도는 같고 구현을 공유하지 못하는 상태다.
> → 발표에서 "플룸이 에이전트에 들어간다"고 하면 사실과 다르다.

### 1-3. D 내부 — 버튼 하나 누르면

```
사용자 입력           도구 4개 (고정 순서)        계산                  확정          설명
──────────────       ───────────────────       ─────────────         ─────────     ────────
작업유형 5개 택1  ─▶  ① get_farm_config    ─▶  두 그룹 중 max    ─▶  추천 Top3 ─▶  Gemini
저장경과일 슬라이더    ② get_risk_calendar      × 시간가중치 6칸       회피 Top3     설명만
Gemini 토글           ③ get_storage_days       × 작업유형 [C]                       (선택)
                     ④ search_rag             × 저장일 계수 [C]      ↑
✗ 텍스트 입력창 없음                                                 숫자는 여기서 확정
```

**Gemini가 스스로 도구를 고르지 않는다.** 애플리케이션 코드가 4개를 정해진 순서로 부른다.
`tools_schema.py` 에 JSON 스키마를 미리 붙여둔 건, 나중에 function calling으로 전환하기 위한 준비다.

---

## 2. 데이터 → 필터링 → 전처리 → 모델 입력

### 2-1. 원본 데이터셋 4종

| # | 데이터 | 출처 | 규모 | 역할 |
| --- | --- | --- | --- | --- |
| ① | 익산시 악취 민원 | 익산시 (재크롤링 2019-05-28~2026-08-17) | **16,113행** | **정답 라벨(y)** |
| ② | 전주 ASOS 146 시간관측 | 기상청 API허브 | **66,882행** (2019-01~2026-08) | **피처(X)** |
| ③ | 익산·김제 축산농가 현황 | 지자체 공공데이터 | 익산 1,166 / 김제 1,511 | **공간 피처** |
| ④ | 법령·매뉴얼 PDF | 국가법령정보센터·축산환경관리원 | 12개 문서 | **RAG 지식베이스** |

보조: 익산 AWS 702(플룸 방향 판정용), AI Hub 양돈 센서(검증용).

### 2-2. 필터링 — 16,113 → 7,547

```
민원 원본                             16,113행
  │
  ├─ ① 익산시 필터 (시군구 == "익산시")   14,994행   (-1,119, 완주군 등 제외)
  │       └ 삼례읍(완주군) 647건은 여기서 걸러진다
  │
  ├─ ② 악취종류코드 == 101 (가축분뇨)     7,547행   (-7,447, 음식점·하수구 등 제외)
  │
  └─ ③ 중복 제거 (동일 신고 10분 내)      → 전체 기준 665건 제거
```

**핵심 설계**: 필터 대상 행을 **버리지 않고 `is_iksan` / `is_livestock` 플래그로 보존**한다.
나중에 "완주군을 넣으면 어떻게 되나" 같은 질문에 재크롤링 없이 답할 수 있다.

**기대 행수를 상수로 못 박아 뒀다** (`config.EXPECT_IKSAN_ROWS = 14994` 등).
필터를 하나 빼먹으면 즉시 콘솔에 `[발견]` 경고가 뜬다.

> ⚠️ **여기서 1,149건(15.5%)이 더 빠진다.** `config.REGION_GROUP` 이 지역 14개만 매핑하는데,
> 매핑 안 된 지역(신동·신흥동·금강동·인화동2가 등)의 민원은 학습에서 제외된다.
> 그중 **47%(535건)는 `REGION_GROUP_R`(21개 확장 매핑)에 이미 정의돼 있다** — 지역격자 실험
> 트랙에서만 쓰고 본 트랙에 안 쓰는 상태다. 파이프라인이 스스로 경고를 띄운다.

### 2-3. 전처리 — 4단계

**① 기상 정제** (`preprocess/clean_data.py`)
- 결측 171시각 → 보간 후 107시각
- **풍향은 순환값이라 그냥 평균내면 안 된다.** 0°와 359°의 평균이 180°(정반대)가 되어버린다.
  → `wd_sin`, `wd_cos` 로 분해해서 보간한다.

**② 김제 농가 재지오코딩** (`preprocess/geocode_gimje.py`)
- 원본 좌표 보유율 **12.2%** → 리(里) 단위 중앙값 대체로 **95.9%** (1,449/1,511)
- 등급 표기: 실좌표 `[A]` 184곳 / 리 중앙값 `[B]` 975곳 / 읍면 중앙값 `[C]` 290곳

**③ 격자 생성 + 라벨링** (`preprocess/build_grid.py`)
```
1행 = (1시간 × 수용점 유형)
기간  2019-05-28 ~ 2026-08-18 = 63,331시각
유형  농촌근거리 / 시가지원거리 2종
──────────────────────────────────────
126,662행 → 기상 결측 214행 제거 → 126,448행
```
- `y_bin` = 그 시각 그 유형에 민원이 났는가 (0/1)
- `y_sev` = 악취 강도 1~5 → **`sample_weight` 로 투입** (강한 민원을 더 무겁게 학습)

**④ 피처 생성** (`spatial_features.py` → `build_features.py`)

### 2-4. 최종 모델 입력 — 28개 피처

| 묶음 | 개수 | 피처 | 설계 의도 |
| --- | --- | --- | --- |
| 기상 | 6 | `temp` `humid` `ws` `wd_sin` `wd_cos` `rain` | 예보 API의 TMP·REH·WSD·VEC·PTY와 **1:1 대응** |
| 야간 | 3 | `night` `night_ws` `night_humid` | 야간 역전층 — 상호작용항 |
| 시각 | 4 | `hour` `month_sin` `month_cos` `dow` | 계절은 순환값이라 sin/cos |
| 시차·무풍 | 5 | `ws_lag1` `ws_lag2` `wd_sin_lag1` `wd_cos_lag1` `calm_streak` | 냄새는 즉시 안 온다. 무풍 누적시간 |
| 공간 | 7 | `up_ik_pig` `up_ik_cattle` `up_ik_poultry` `up_gj_*` `up_nearest_km` | **풍상측 ±30°·15km 내 농가 수** |
| 과거율 | 3 | `prior_rate_1y` `prior_std_1y` `prior_month` | 직전 1년 민원율 (`shift(1)` 후 rolling) |

**공간 피처가 이 프로젝트의 핵심 아이디어다.** "바람이 부는 쪽 상류에 농가가 몇 개나 있나"를
순수 기하로 계산한다 — 플룸(물리 모델)이 아니라 **부채꼴 카운트**다.

**누수 방지 2가지:**
- `prior_rate_1y` 는 반드시 `shift(1)` 후 rolling — 자기 행을 포함하면 정답을 보는 것
- 시차 피처는 `groupby` 안에서 계산 — 안 그러면 A그룹 마지막 행이 B그룹 첫 행을 참조

**중기예보용 reduced 모델은 7개만 쓴다** (`temp` `rain` `hour` `night` `month_sin` `month_cos` `dow`).
D+4~7 중기예보는 **일 단위라 바람이 없기 때문**이다.

### 2-5. 분할 — 시계열 분할 (랜덤 분할 아님)

```
train  ≤ 2023    40,194행
valid   2024      8,784행
test    2025      8,733행     ← 그룹당. 총 2배
```

| 그룹 | 양성률 | train 양성 | valid 양성 | test 양성 |
| --- | --- | --- | --- | --- |
| 농촌근거리 | 0.0156 (988/63,224) | 270 | 361 | 256 |
| 시가지원거리 | 0.0369 (2,331/63,224) | 1,813 | 164 | 251 |

전체 양성률 **2.62%** — 불균형 데이터다.

---

## 3. ML과 플룸의 OUTPUT

### 3-1. ML(XGBoost) 출력 — 판단의 주체

**모델 4개**: (농촌근거리 / 시가지원거리) × (full 28피처 / reduced 7피처)

**최종 산출물:**
```
model_{유형}_full.pkl      model_{유형}_reduced.pkl
grade_cuts_{유형}.json     ← 월별 등급 컷 12개
group_center.json          ← 학습에 쓴 중심좌표 (서빙과 반드시 일치해야 함)
```

**성능 (2026-08-23 실측):**

| 그룹 | 분할 | ROC-AUC | PR-AUC | lift | 상위20% 재현율 | A등급 민원율 |
| --- | --- | --- | --- | --- | --- | --- |
| 시가지원거리 | valid 2024 | 0.8845 | 0.1872 | 10.03배 | 80.5% | 평균의 1/16.4 |
| **시가지원거리** | **test 2025** | **0.8977** | **0.2161** | **7.52배** | **83.3%** | **평균의 1/25.1** |
| 농촌근거리 | valid 2024 | 0.7968 | 0.1508 | 3.67배 | 58.7% | 평균의 1/6.6 |
| 농촌근거리 | test 2025 | 0.7442 | 0.0770 | 2.63배 | 48.8% | 평균의 1/6.4 |

중기용 reduced: 시가지 test PR-AUC 0.1755(lift 6.11배) / 농촌 0.0827(2.82배)

> **`lift` 가 가장 설득력 있는 숫자다.** PR-AUC 0.2161을 양성률 0.0287로 나눈 값이 7.52배 —
> "무작위로 찍는 것보다 7.5배 잘 맞힌다"는 뜻이다.
> **`A등급 시각의 민원율이 전체 평균의 1/25`** 이 발표용으로 가장 직관적이다.

**서빙 출력** (`serving/db.py` → `risk_hourly` 테이블):
```
PK(date, hour, grp)   ← grp 없으면 한 유형이 다른 유형을 덮어쓴다
확률(0~1) + 등급(낮음/주의/위험)
```
2026-08-23 실행: 단기 66시각 + 관측꼬리 48시각 = 132행, 중기 192행, **누적 322행**
등급 분포 `{낮음 139, 주의 48, 위험 135}`

### 3-2. 플룸(가우시안) 출력 — 대상 선택만

**플룸은 등급을 바꾸지 않는다. `PLUME_GRADE_BUMP = False` 는 영구 OFF다.**

출력은 **"어느 수용점 유형을 볼지"** 하나뿐이다 (`analysis/plume_select.py`):

```
농가 좌표 + 풍향 + 풍속 + 운량
        ↓
   Pasquill 안정도 등급 (A~F)
        ↓
   ≤ 3km  → 플룸 유효 반각을 그대로 사용
   > 3km  → 고정 섹터 ±30° 로 대체 (플룸 가정이 깨지는 거리)
        ↓
출력: ['시가지원거리']  ← 이것뿐. 점수도 등급도 아니다
```

2026-08-23 실행 결과: **144개 후보 시각 중 플룸이 그룹을 좁힌 것은 3개(2%)**.
나머지 141개는 풍하측에 해당 그룹이 없어 전 그룹 최댓값을 썼다.

### 3-3. 왜 곱하지 않는가 — 절대규칙 1

```
물리 정보가 들어가는 자리는 딱 둘까지다:

  ① 피처 (풍상측 노출)   수용점 기준 "어디서 오나"   학습 단계   ✅
  ② 조합 (유형 선택)     발원 기준   "어디로 가나"   서빙 단계   ✅
  ③ 등급 보정 (BUMP)    최종 등급 가감              추천 단계   ❌ 영구 OFF
```

①과 ②는 **방향도 단계도 다르므로 이중 계산이 아니다.** ③까지 켜면 삼중 계산이다.
바람과 주야간이 ML 피처에도 플룸에도 들어가 있어서, 곱하면 같은 정보를 두 번 세는 셈이 된다.

v5 검증 실측: lift 1.61(PASS) / 이탈각 70.5°(FAIL, 기준 70) / 풍하측 적중 0.5759(FAIL, 기준 0.60)
→ **2/3 항목이 기준 미달이라 등급 보정으로 쓰기엔 근거가 부족했다.**

### 3-4. 최종 추천 출력

2026-08-23 실행 (돼지 농가 · 액비살포):
```
후보 144시각
추천 : 2026-08-25 07시   final 0.7617  등급 주의  A등급
       플룸 → 풍하측 시가지원거리
       그룹별 창위험도 {농촌 0.7908, 시가지 0.5078} → 채택 [시가지원거리]
회피 : 2026-08-28 21시   final 1.4524  등급 위험  C등급
A/B/C 분포 {A:29, B:43, C:72}
조언 : 주입식으로 바꾸면 배출량이 57% 감소합니다
       살포 후 즉시 경운하면 38% 감소합니다
```

**등급이 두 층인 이유:**
- `위험/주의/낮음` = 월별 절대 컷 (계절 보정. 여름은 원래 다 위험하다)
- `A/B/C` = 후보 144개 안에서의 **상대 순위** (농가는 "이 중에 언제가 최선인가"를 알고 싶다)

**배출량 kg 절대값은 출력하지 않는다** — 상대 감소율(%)만 낸다. 절대값은 신뢰할 근거가 없다.

---

## 4. B·C 결과가 Agent와 어떻게 연결되는가

### 4-1. 절대규칙 3 — 판단은 코드, 설명은 LLM

```
   ┌──────────────────────────────────────────────────┐
   │  판단 계층 — 코드가 확정한다                       │
   │    B: risk_hourly (1시간 위험값)                  │
   │    D: work_guide.py 결정론적 계산 → 추천/회피 Top3 │
   └──────────────────────┬───────────────────────────┘
                          │  확정된 결과 (변경 불가)
                          ▼
   ┌──────────────────────────────────────────────────┐
   │  설명 계층 — 판단하지 않는다                       │
   │    C: RAG 근거 조문 검색                          │
   │    D: Gemini 서술                                 │
   │    ★ 틀려도 추천 시각과 점수가 안 바뀐다            │
   └──────────────────────────────────────────────────┘
```

### 4-2. 연결 고리 — `DecisionProvider` 계약

D는 A/B/C를 **직접 부르지 않는다.** `agents/contracts.py` 의 dataclass 계약만 본다.

```python
SourceInfo    state: fixture | connected | stale | unavailable
WorkWindow    start, end, window_risk, recommendation_score, grade, reasons
GuideCard     farm_id, work_type, recommended[3], avoid[3], before/after_actions
```

provider 구현 3종을 교체해서 쓴다:
| 구현 | 연결 대상 |
| --- | --- |
| `legacy_provider.py` (기본) | `serving.db` 의 `risk_hourly` + `rag_yujin` RAG |
| `fixture_provider.py` | 고정 시연 데이터 (구조 검증용) |
| 외부 factory | `D_PROVIDER_FACTORY=package.module:create_provider` |

**연결 실패를 fixture로 조용히 바꾸지 않는다.** 실패는 실패로 드러낸다.

### 4-3. B의 1시간 위험값 → D의 6시간 작업 계획

```python
# agents/scoring_policy.py
TIME_WEIGHTS = (0.30, 0.22, 0.17, 0.13, 0.10, 0.08)   # 작업 후 0~5시간
WORK_WEIGHT  = {"분뇨제거":1.3, "청소":1.0, "환기점검":0.7,
                "저감시설점검":0.8, "액비살포":1.5}      # [C] 근거 미확보
storage_factor(days) = 1.0 if days < 14 else 1.5       # [C]

window_risk          = Σ(risk_hourly[i] × TIME_WEIGHTS[i])   # i = 0..5
recommendation_score = window_risk × WORK_WEIGHT × storage_factor
```

**설계 포인트:**
- 1시간 위험값 6개에 가중치를 **직접** 적용한다. 3시간 평균으로 먼저 뭉개지 않는다.
- 연속 6시간이 전부 있어야만 창을 만든다 (`timedelta(hours=1)` 검사)
- **일 단위 중기예보(D+4~7)는 시간 추천 후보에서 제외한다** — 없는 시간 정밀도를 화면에서 만들지 않는다
- `[C]` 가중치는 화면·발표에서 **가정임을 계속 표시**한다

### 4-4. RAG가 붙는 자리

`agents/rag_adapter.py` 가 `rag_yujin` 검색기를 D 계약으로 변환한다.
백엔드는 `RAG_BACKEND` 로 고른다 — `pgvector`(Supabase, 배포용) / `chroma`(로컬, 오프라인용).
두 백엔드는 같은 청킹·같은 임베딩이라 결과가 동일하다.

```
work_guide 가 확정한 작업유형("분뇨제거")
        ↓
RagYujinAdapter.search("분뇨제거 ...")   backend="rag_yujin-pgvector"
        ↓
manual 검색 3건 + law 검색 3건 (doc_type 분리)
        ↓
GuideCard.reasons 에 출처 메타데이터로 첨부
   source_file / unit(제N조·별표N) / page / doc_type
```

**RAG가 없으면 어떻게 되나 — 실제 실행 결과:**
```
[판단 근거]
  - 법령·매뉴얼 검색 결과 없음: 공식 원문 확인 필요
  - 한계: 사용 불가 provider: rag
  - 한계: C 근거 검색 결과가 없어 작업 전 공식 원문 확인 필요
```
**추천 시각과 점수는 그대로 나온다.** RAG는 근거를 덧붙일 뿐 판단에 관여하지 않는다.
Gemini도 마찬가지 — `GOOGLE_API_KEY` 가 없으면 설명 없이 동일한 확정 계획을 출력한다.

---

## 5. RAG 구현

### 5-1. 지식베이스 — 12개 문서

| 유형 | 개수 | 내용 |
| --- | --- | --- |
| 법령 (`law`) | 6 | 악취방지법 (법률·시행령·시행규칙), 가축분뇨법 (법률·시행령), 익산시 조례 |
| 매뉴얼 (`manual`) | 6 | 축산농장 악취저감시설 운영 매뉴얼(돼지), 축산악취 관리 지침서, 양돈장 냄새 실천방안, 자연순환 양돈분뇨처리, 정제텍스트 2종 |

**데이터를 `rag_yujin/data/핵심자료/` 안에 뒀다** — 팀 공용 `프로젝트 데이터/` 폴더 구조가
두 번 바뀐 전례가 있어서, 코드와 데이터를 같은 트리에 두고 상대경로 의존을 끊었다.

원본 상위 폴더의 나머지 12개는 **의도적으로 제외**했다 — 별표 스텁(제목만 있고 표 내용 없음),
손상 파일, 무관 법령, 중복.

**실측 (2026-08-23 빌드)**: 문서 12개 → 페이지 294개 → **청크 567개** → Chroma 567벡터 (53초)

| 문서 | 청크 | 문서 | 청크 |
| --- | --- | --- | --- |
| 악취방지법 시행규칙 | 115 | 축산악취 관리 지침서 | 115 |
| 가축분뇨법(법률) | 103 | 악취방지법(법률) | 47 |
| 가축분뇨법 시행령 | 46 | 악취방지법 시행령 | 27 |
| 익산시 조례 | 26 | 축산농가 냄새저감 공식 | 23 |
| 붙임1 저감시설 매뉴얼(돼지) | 20 | 자연순환 양돈분뇨처리 | 19 |
| 026_양돈장 냄새발생원인 | 14 | 냄새저감 기본관리 매뉴얼 | 12 |

**로더**: `PyMuPDFLoader` (PyPDFLoader보다 한글 추출 품질이 안정적), `.txt`는 `TextLoader(utf-8)`
- 페이지당 평균 50자 미만이면 "텍스트 추출 부실" 경고 → 별표 스텁을 조기 검출

### 5-2. 청킹 — 문서 유형별 2-트랙 (핵심 차별점)

**`RecursiveCharacterTextSplitter` 를 그냥 쓰지 않았다.** 글자 수로만 자르면
`"제7조에 따른 기준"` 같은 **본문 내 조문 참조**와 **실제 조문 시작**을 구분하지 못해
법령 조문이 중간에 잘린다.

```
law    → 정규식으로 제N조 / 별표N 경계 우선 분할 (글자수로 절대 안 자름)
         ARTICLE_RE = (?m)(?=^\s*제\s*\d+\s*조(?:의\s*\d+)?(?![가-힣\d])...)
                          ↑ 줄 시작(^)에서만 매치 — 본문 내 참조 오인 방지
         → 결과가 910자 초과일 때만 2차 보조 분할

manual → 소제목(장/절/번호/■) 경계 우선 분할
         → 결과가 910자 초과일 때만 2차 보조 분할
```

| 파라미터 | law | manual | 근거 |
| --- | --- | --- | --- |
| `CHUNK_SIZE` | 700자 | 700자 | ko-sroberta **512 토큰 한계**를 넘지 않도록 보수적으로 |
| `CHUNK_OVERLAP` | 100자 | 80자 | 조문은 항·호가 이어져 겹침을 더 줌 |
| `SPLIT_THRESHOLD` | 910자 (700×1.3) | 910자 | 이 길이를 넘는 것만 2차 분할 |
| `MIN_CHUNK_LEN` | 40자 | 40자 | 파편 제거 |

**700자 상한을 넣은 이유 — 실측으로 발견했다.** 원래 조문 경계 분할만 했더니
`제16조의6` 이 **10,051자**, `별표3` 이 **6,424자** 짜리 단일 청크로 남아 있었다
(`_peek_chunk_sizes.py` 로 확인). 임베딩 하나가 서로 다른 여러 항·호를 뭉뚱그려
검색 정밀도와 재현율을 둘 다 깎아먹는다.

**실전에서 잡은 함정 3가지:**
1. `MAX_HEADING_LABEL_LEN = 30` — `"2) 관행수준의 조단백질 함량：비육전기(50~80kg) 19%..."`
   같은 긴 본문이 소제목으로 오인된다. 30자 넘으면 `"본문"` 이라는 정직한 라벨로 대체.
   *거짓으로 구체적인 척하는 출처 라벨보다 낫다.*
2. `PAGE_MARKER_RE` — `TextLoader` 는 페이지 메타데이터를 안 준다. 정제 텍스트에 심어둔
   `## N쪽` 표시로 페이지를 복원. 안 그러면 그 문서의 **모든 청크가 page=1로 뭉개진다.**
3. **중복 제거는 문서 "안"에서만** — 전역으로 하면 조문이 거의 같은 별개 조례(구례군/완주군/정읍시)에서
   뒤에 처리된 문서의 정당한 내용이 조용히 통째로 사라진다.

### 5-3. 임베딩 & 벡터 DB

| 항목 | 값 | 선택 이유 |
| --- | --- | --- |
| 모델 | `jhgan/ko-sroberta-multitask` | KorSTS/KorNLI 검증된 한국어 특화 모델. **로컬 무료** — API 비용 없이 재현 가능 |
| 차원 | 768 | |
| 정규화 | `normalize_embeddings=True` | 코사인 유사도 계산 정확도 |
| 디바이스 | CPU (기본) | `EMBEDDING_DEVICE=cuda` 로 전환 가능 |
| 벡터 DB | Chroma (`livestock_odor_kb`) | 로컬 persist, 재생성 가능 |

**저장할 때와 검색할 때 반드시 같은 임베딩 함수를 쓴다** (`get_embedding_function()` 하나로 통일).
서로 다른 모델을 쓰면 벡터 공간이 어긋나 검색이 아예 안 된다.

### 5-4. 검색 — manual/law 분리 검색

```python
manual_retriever = search_kwargs={"k": 3, "filter": {"doc_type": "manual"}}
law_retriever    = search_kwargs={"k": 3, "filter": {"doc_type": "law"}}
```

**왜 분리했나** — 한 번에 top-6을 뽑으면 한쪽 문서 유형이 우연히 유사도가 높을 때
다른 쪽이 결과에서 **아예 빠진다.** 농가는 "실무 요령"과 "법적 기준"을 **둘 다** 알아야 하므로
각각 최소 3건씩 보장한다.

**LLM**: Gemini `gemini-3.5-flash`, `temperature=0.2` (창의성이 아니라 재현성이 목적)
**출력 형식**: `1) 실무 요령` / `2) 관련 법령` 2섹션 고정 + `[실무-N]` `[법령-N]` 인용 번호

### 5-5. 평가 ① 규칙 기반 — 20문항

작업유형 4종 × 5문항. 기계적으로 확인 가능한 것만 자동 채점한다.

| 체크 | 내용 |
| --- | --- |
| `manual_hit` / `law_hit` | 실무·법령 자료가 하나라도 검색됐는가 |
| `citation_ok` | 답변의 `[실무-N]` 번호가 **실제 검색된 자료 개수 범위 안**인가 (없는 `[실무-5]` 를 지어내면 걸림) |
| `has_both_sections` | 두 섹션이 다 있는가 |

**결과: 19/20 통과.** 실패 1건은 `"안개분무 시설의 노즐은 어떻게 관리해야 하나요?"` —
실무·법령 검색이 둘 다 비었다.

> ⚠️ 이건 **구조적 이상만** 걸러낸 것이다. 답변 "내용"이 맞다는 보장은 아니다.

### 5-6. 평가 ② RAGAS — 자동 평가

**RAGAS가 뭔가**: 사람이 만든 정답셋 없이 RAG 성능을 자동 채점하는 오픈소스 프레임워크.
LLM이 문서로 지식그래프를 만들어 **질문과 참조 정답을 자동 생성**하고, 그 답을 LLM이 채점한다.

**4개 지표 — 검색과 생성을 분리해서 본다:**

| 지표 | 무엇을 보나 | 낮으면 |
| --- | --- | --- |
| `faithfulness` | 답변이 **검색된 문맥에만** 근거하는가 | **환각** |
| `answer_relevancy` | 답변이 질문에 맞는가 | 동문서답 |
| `context_precision` | 검색 결과 상위가 **정답과 관련 있나** | 검색 노이즈 |
| `context_recall` | 정답에 필요한 문맥을 **다 찾아왔나** | 검색 누락 |

**결과 (12문항, 분뇨제거·환기점검 샘플 — 전체 567청크 중 일부):**

| 지표 | `ragas_eval_report.md` | `_6_6_ragas_eval_results.md` |
| --- | --- | --- |
| faithfulness | **0.699** | **0.771** |
| answer_relevancy | **nan** | **nan** |
| context_precision | **0.625** | **0.365** |
| context_recall | **0.750** | **0.569** |

> ⚠️ **리포트가 2개고 값이 다르다.** `_6_2_RAGAS_test.py` 가 쓰는 파일은
> `ragas_eval_report.md` 이므로 그쪽이 최신 실행 결과다. 발표 땐 **하나만** 인용할 것.

**해석 (`ragas_eval_report.md` 기준):**
- `faithfulness 0.699` — 답변의 70%가 검색 문맥에 충실. **자료에 없으면 "찾지 못함"이라고 정직하게 답한다** (12번 문항이 실제 그렇게 나왔다)
- `context_recall 0.750` — 검색이 필요한 근거의 3/4을 찾아온다
- `context_precision 0.625` — 검색 상위 결과의 정밀도. 개선 여지가 가장 큰 지표
- `answer_relevancy nan` — **측정 실패**. 미해결

**왜 12개만 돌렸나 (선제 방어)**: RAGAS 테스트셋 자동 생성은 문서 단위로 LLM을 여러 번 호출한다.
전체(567청크)를 넣으면 Gemini 무료 티어 요청 제한에 걸린다.
**목적은 전체 커버리지가 아니라 "파이프라인이 실제로 맞물려 도는가"의 최소 단위 증명(PoC)이다.**

**RAGAS 자체 버그도 하나 잡았다**: `ragas 0.4.3` 의 `default_transforms()` 는 입력 문서의 25%가
500토큰을 넘으면 `HeadlineSplitter` 경로를 켜는데, 이 splitter가 500토큰 이하 문서에도 적용되면서
`'headlines' property not found` 로 죽는다. 라이브러리 소스에서 직접 확인하고 우회했다.

---

## 6. 예상 Q&A 20선

### 데이터·모델링

**Q1. 왜 XGBoost인가요? 딥러닝은 안 쓰나요?**
> 표 형식 데이터에 12만 행, 양성 3,319건입니다. 이 규모에서 딥러닝은 XGBoost를 못 이깁니다.
> 더 중요한 건 **설명 가능성**입니다. 농가와 지자체에 "왜 이 시각이 위험한가"를 답해야 하는데
> SHAP으로 피처 기여도를 바로 보여줄 수 있습니다. 그리고 재현성 — 시드 고정하면 항상 같은 결과가 나옵니다.

**Q2. 왜 피처를 저렇게 구성했나요?**
> 원칙이 하나입니다 — **기상청 단기예보 API가 주는 변수만** 씁니다.
> 기상 6개(temp·humid·ws·wd_sin·wd_cos·rain)는 예보의 TMP·REH·WSD·VEC·PTY와 1:1 대응합니다.
> 나머지 22개는 그 6개에서 **파생**시킨 겁니다 — 시차(1·2시간 전 바람), 야간 상호작용,
> 계절 순환(sin/cos), 풍상측 농가 수, 직전 1년 민원율.
> 즉 **예보에 없는 정보는 단 하나도 안 들어갑니다.**

**Q3. 암모니아(NH3) 농도를 왜 안 넣었나요? 악취의 직접 원인 아닌가요?**
> 맞습니다. 그래서 넣어봤고, 성능도 올랐습니다. 그런데 **예보가 존재하지 않습니다.**
> 학습은 되는데 예측을 못 하는 모델이 됩니다. "3일 뒤 암모니아 농도"를 아무도 알려주지 않으니까요.
> 이건 저희 절대규칙 2번이고, 실제로 사고가 난 뒤에 생긴 규칙입니다.

**Q4. 양성률이 2.6%인데 불균형 처리는 어떻게 했나요?**
> 오버샘플링(SMOTE)은 **안 했습니다.** 시계열 데이터에서 합성 샘플을 만들면 시간 구조가 깨집니다.
> 대신 두 가지를 썼습니다. ① `scale_pos_weight` 로 클래스 가중치를 조정하고,
> ② **악취 강도 1~5를 `sample_weight` 로 투입**했습니다. 강한 민원을 더 무겁게 학습합니다.
> 평가도 accuracy가 아니라 **PR-AUC와 lift**로 봅니다. 2.6% 불균형에서 accuracy는 무의미합니다.

**Q5. y=0이 "안전했다"인지 "아무도 안 뿌렸다"인지 어떻게 구분하나요?**
> **구분할 수 없습니다. 이게 이 프로젝트의 가장 큰 구조적 한계입니다.**
> 살포 기록이 세상에 존재하지 않습니다. 그래서 저희는 **인과가 아니라 상관을 학습**하고 있고,
> 그 사실을 숨기지 않습니다. 그래서 "민원을 줄여준다"고 말하지 않고
> **"상대적으로 위험한 시각을 피하도록 돕는다"** 고 말합니다.

**Q6. 왜 랜덤 분할이 아니라 연도로 나눴나요?**
> 랜덤 분할하면 **미래로 과거를 예측**하게 됩니다. 2025년 데이터로 학습해서 2023년을 맞히는 건
> 실전에서 불가능한 상황이고, 성능이 부풀려집니다.
> train ≤2023 / valid 2024 / test 2025로 **시간 순서를 지켰습니다.**
> 실제 운영과 같은 조건입니다.

**Q7. 수용점 유형을 왜 2개로 나눴나요?**
> 발원지로부터의 **거리가 다르면 지배하는 물리가 다르기 때문**입니다.
> 농촌근거리는 0.2~2km, 시가지원거리는 5~14km입니다.
> 도달 시간이 다르니 어떤 바람 조건이 위험한지도 달라집니다.
> 실제로 물리 노출 지수 대비 민원 수 상관이 0.099밖에 안 나왔습니다 —
> 하나의 모델로 뭉치면 양쪽 다 못 맞힙니다. 성능도 시가지 0.90 vs 농촌 0.74로 확연히 다릅니다.

**Q8. 왜 익산 AWS가 더 가까운데 전주 ASOS를 썼나요?**
> 실측으로 비교했습니다. 전주 ASOS가 시가지 test PR-AUC 0.2197, 익산 AWS가 0.2138로 전주가 이겼습니다.
> **방향 신호 자체는 익산이 오히려 낫습니다**(풍향 lift 16.6배 vs 16.3배). 문제는 **무풍 비율**입니다.
> 익산 AWS는 무풍(<0.5m/s)이 16.5%로 전주(7.1%)의 2.3배입니다.
> 무풍 구간에서 풍향은 사실상 난수인데, 공간 피처가 그 난수로 부채꼴을 그립니다.
> 결측도 12배 많습니다(408 vs 34시각, 무인 관측이라).
> 역설적이지만 **전주의 20도 양자화가 국지 난류를 걸러내는 스무딩으로 작동**한 것으로 봅니다.
> 다만 **플룸 방향 판정에는 익산 AWS를 씁니다** — 역할을 나눴습니다.

**Q9. 농촌근거리 성능이 낮은데(0.74) 왜 그런가요?**
> 원인은 파악돼 있습니다. **왕궁면 흥암리 민원이 2020~2023 통틀어 1건이었는데 2024년에 517건으로 급증**했습니다.
> 학습 구간과 평가 구간의 지역 구성이 아예 달라진 겁니다.
> 원인이 앱 보급인지, 지역명 표기 변경인지, 실제 사건인지는 아직 확인이 필요합니다.
> 모델 문제라기보다 **데이터 분포 변화(distribution shift)** 입니다.

**Q10. 학습은 관측 데이터인데 서빙은 예보입니다. 성능이 떨어지지 않나요?**
> 떨어집니다. 그리고 **얼마나 떨어지는지 아직 측정하지 못했습니다.** 정직하게 말씀드리는 한계입니다.
> 과거 단기예보 자료를 확보하면 백테스트할 수 있는데 아직 못 구했습니다.
> 대신 완화 장치는 넣었습니다 — 서빙 시 **관측 마지막 48시간을 이어붙입니다.**
> 안 그러면 `ws_lag1`, `calm_streak` 가 0에서 시작해서 학습 분포와 어긋납니다.
> 그리고 `run_check.py` 가 매번 **서빙 피처 분포가 학습 범위 안에 있는지** 검사합니다(현재 100%).

**Q11. 확률값 0.23 같은 숫자는 믿어도 되나요?**
> **아니요. 순위만 믿으셔야 합니다.**
> 확률이 보정(calibration)되지 않았습니다. `scale_pos_weight` 를 썼기 때문에 확률이
> 실제 양성률 대비 8~16배 부풀려져 있습니다.
> 그래서 화면에 절대 확률을 안 보여주고 **등급(A/B/C)과 순위만** 보여줍니다.

**Q12. 등급 컷을 왜 월별로 나눴나요?**
> 여름은 원래 다 위험하기 때문입니다. 연간 고정 컷을 쓰면 **7~8월이 전부 "위험"으로 도배**되고
> 겨울은 전부 "낮음"이 됩니다. 농가한테 아무 정보가 안 됩니다.
> 월별 컷을 쓰면 "8월 중에서 상대적으로 안전한 시각"을 고를 수 있습니다.
> 그래서 등급이 두 층입니다 — 월별 절대 컷(위험/주의/낮음) + 후보 내 상대 순위(A/B/C).

### 플룸·구조

**Q13. 플룸(물리 모델)을 왜 쓰나요? ML만으로 충분하지 않나요?**
> 플룸은 **등급을 만들지 않습니다.** "어느 수용점 유형을 볼지" 고르는 데만 씁니다.
> ML은 "농촌근거리 0.79 / 시가지원거리 0.51" 두 점수를 다 내놓는데,
> 지금 바람에 이 농가 냄새가 **어느 쪽으로 가는지**는 ML이 모릅니다. 그걸 플룸이 정합니다.
> 실제로 144개 후보 중 3개(2%)에서만 그룹을 좁혔습니다 — 기여가 작다는 것도 정직하게 말씀드립니다.

**Q14. 그럼 ML 점수와 플룸 결과를 곱하면 더 정확하지 않나요?**
> **이중 계산이 됩니다.** 바람과 주야간이 ML 피처에도 들어가 있고 플룸에도 들어가 있습니다.
> 곱하면 같은 정보를 두 번 세는 겁니다.
> 물리 정보가 들어가는 자리를 딱 둘로 제한했습니다 — ① 피처(풍상측 노출, 수용점 기준),
> ② 조합(유형 선택, 발원 기준). 방향도 단계도 다르므로 이중 계산이 아닙니다.
> 등급 보정까지 켜면 삼중이 됩니다. 검증에서도 3개 기준 중 2개가 미달이라 영구 OFF했습니다.

**Q15. 3km를 기준으로 플룸과 섹터를 나눈 근거는?**
> 가우시안 플룸은 **정상상태·균일풍 가정** 위에 서 있습니다. 거리가 멀어질수록 이 가정이 깨집니다.
> 3km를 넘으면 이동 중 풍향이 바뀌고 지형 효과가 끼어들어서 플룸 반각이 의미를 잃습니다.
> 그래서 3km 초과는 고정 섹터 ±30°로 대체합니다. 공간 피처와 같은 각도를 써서 일관성을 유지합니다.

### RAG

**Q16. 청킹 사이즈를 왜 700자로 했나요?**
> 임베딩 모델 `ko-sroberta` 의 **512 토큰 한계** 때문입니다. 한국어는 대략 1.3~1.5자당 1토큰이라
> 700자면 안전 범위입니다.
> 다만 **700자는 상한이지 기본 단위가 아닙니다.** 법령은 조문 경계로 먼저 자르고,
> 그 결과가 910자(700×1.3)를 넘을 때만 2차로 700자 분할합니다.
> 이 상한이 원래 없었는데, 실측해보니 `제16조의6` 이 10,051자짜리 단일 청크로 남아 있었습니다.
> 그렇게 길면 임베딩 하나가 여러 항·호를 뭉뚱그려서 정밀도·재현율을 둘 다 깎습니다.

**Q17. 왜 그냥 RecursiveCharacterTextSplitter를 안 쓰고 정규식을 썼나요?**
> 글자 수 기준으로 자르면 **조문이 중간에 잘립니다.**
> 더 나쁜 건 `"제7조에 따른 기준을 적용한다"` 같은 **본문 내 조문 참조**를
> 새 조문의 시작으로 오인하는 겁니다. 그러면 엉뚱한 데서 청크가 갈립니다.
> 그래서 정규식에 `(?m)^` 를 붙여 **줄 시작에서만** 매치하게 했습니다.
> 법령 답변은 정확도가 생명이라 구조 기반 분할을 우선했습니다.

**Q18. RAGAS 점수가 높은 편인가요? faithfulness 0.699면 30%가 환각 아닌가요?**
> 절대 기준이 있는 지표는 아니고, 개선 여지가 있는 건 맞습니다.
> 다만 낮게 나온 문항들을 열어보면 상당수가 **"자료에 없어서 못 찾았다"고 정직하게 답한 경우**입니다.
> 8번 문항이 faithfulness 0.000인데, 답변이 `"관련 자료를 찾지 못함"` 이었습니다.
> 지어내는 것보다 훨씬 낫습니다.
> 그리고 **12문항 PoC**라는 점을 감안해주셔야 합니다. 전체 커버리지 평가가 아니라
> 파이프라인이 맞물려 도는지 증명하는 최소 단위입니다.
> 무료 티어 요청 제한 때문에 전체(567청크)를 못 돌립니다.

**Q19. LLM이 틀린 말을 하면 농가가 잘못된 판단을 하지 않나요?**
> **추천 시각과 점수는 LLM이 못 바꿉니다.** 이게 저희 절대규칙 3번입니다.
> Gemini는 이미 코드가 확정한 결과를 **설명만** 합니다.
> `GOOGLE_API_KEY` 를 빼고 돌리면 설명 없이 **똑같은 추천**이 나옵니다.
> RAG도 마찬가지입니다 — 검색이 실패하면 `"사용 불가 provider: rag"` 라고 화면에 찍고
> 추천은 그대로 나옵니다. 조용히 넘어가지 않습니다.
> 그리고 인용 번호가 실제 검색 건수 범위를 벗어나면 자동 검사에 걸립니다.

### 프로젝트 전반

**Q20. 그래서 실제로 민원이 줄어드나요? 검증했나요?**
> **검증하지 못했습니다. 그리고 앞으로도 이 데이터로는 어렵습니다.**
> 살포 기록이 없어서 "추천대로 했더니 민원이 줄었다"를 확인할 방법이 없습니다.
> 저희가 검증한 건 **"A등급 시각의 민원율이 전체 평균의 1/25"** 이라는 통계적 관계입니다.
> 그래서 저희는 "민원을 줄여준다"가 아니라 **"상대적으로 위험한 시각을 피하도록 돕는다"** 고 말합니다.
> 실제 효과 검증은 익산시와 협의해서 시범 농가 몇 곳에 적용해보는 게 다음 단계입니다.

### 보너스 — 나올 수 있는 날카로운 질문

**Q21. 민원의 26%가 김제에서 발원한다면서요? 익산 농가만 관리해서 되나요?**
> 안 됩니다. **그게 이 서비스 효과의 상한선입니다.**
> 저희 모델은 김제 농가도 공간 피처에 넣습니다(`up_gj_pig` 등) — 위험도 예측에는 반영됩니다.
> 하지만 "김제 농가한테 작업 시각을 바꾸라"고 할 권한이 익산시에 없습니다.
> 이건 기술 문제가 아니라 행정 문제이고, 광역 협의가 필요합니다.

**Q22. 야간 민원이 81.5%인데, 정말 밤에 냄새가 심한 건가요?**
> **모릅니다.** 역전층 때문에 실제로 냄새가 정체되는 것일 수도 있고,
> 그냥 사람들이 집에 있어서 신고할 수 있는 시간이라서일 수도 있습니다.
> 저희는 **악취를 예측하는 게 아니라 민원을 예측**하고 있고, 둘은 다릅니다.
> 다만 실용적으로는 구분이 덜 중요합니다 — 농가가 피하고 싶은 건 결국 민원이니까요.

---

## 부록 A. 시연 순서 (실행 시간 포함)

```powershell
cd demo
python run_train.py     # 약 3분   — 학습, out/training_results.json 갱신
python run_serve.py     # 약 30초  — 실 기상청 API 호출 → risk_hourly 적재
python run_check.py     # 약 20초  — 39항목 회귀 점검 (전부 OK 나와야 함)
python tests/test_d_agents.py   # 0.1초 — D 에이전트 11 tests
```

> ⚠️ **`run_train.py` 를 먼저 안 돌리면 나머지가 전부 실패한다.** `data/` 는 gitignore 대상이다.
> ⚠️ **`KMA_KEY` 는 절대 설정하지 말 것.** 환경변수 경로에 `unquote()` 가 빠져 있어
> 설정하면 중기예보가 **조용히** 깨진다. 설정 안 하면 폴백 키로 정상 동작한다.
> ⚠️ `agents/_test_smoke.py` 는 현재 **import 순서 버그로 실행 불가** (`console` 을 `config` 보다 먼저 import).

RAG 시연 (2026-08-23 실행 확인 완료):
```powershell
pip install -r requirements-d.txt        # 약 2분
python rag_yujin/_4_database.py          # 약 1분 — 로더+청커+임베딩+저장을 한 번에
```
`_4_database.py` 하나가 `_1`~`_3` 을 다 호출하므로 순서대로 돌릴 필요 없다.
Chroma DB는 `.gitignore` 대상이라 **clone 직후 없는 게 정상**이고, 위 명령으로 재생성한다.
`_5_search.py`(Gemini 답변 생성)부터는 `GOOGLE_API_KEY` 가 필요하지만,
**검색·인용까지는 API 키 없이 동작한다.**

> ⚠️ `requirements-d.txt` 설치 시 `pyarrow` 20.0.0 → 25.0.1, `requests` 2.32.3 → 2.34.2 로
> 올라가 `requirements.txt` 핀과 어긋난다. **A/B 39항목은 그대로 통과함을 확인했다.**

## 부록 B. 숫자 치트시트

| 항목 | 값 |
| --- | --- |
| 민원 원본 → 익산 → 가축분뇨 | 16,113 → 14,994 → **7,547** |
| 기상 관측 | 66,882행 (2019-01~2026-08) |
| 학습 테이블 | **126,448행** (63,224시각 × 2유형) |
| 피처 | full **28개** / reduced **7개** |
| 양성률 | 2.62% (농촌 1.56% / 시가지 3.69%) |
| 시가지 test | ROC **0.8977** · PR **0.2161** · lift **7.52배** |
| 농촌 test | ROC **0.7442** · PR **0.0770** · lift **2.63배** |
| A등급 민원율 | 전체 평균의 **1/25.1** (시가지 test) |
| 회귀 점검 | **39항목 / 실패 0** |
| RAG 문서 | **12개** (법령 6 + 매뉴얼 6) → 페이지 294 → **청크 567** |
| Supabase | `public`(A/B) + `rag`(C) 두 스키마 · rag 5,504 kB |
| 배포 의존성 | 1,700MB → **283MB** (임베딩 모델·ML 제거) |
| 청킹 | 700자 상한 · overlap 80~100 · 임계 910자 |
| 임베딩 | ko-sroberta-multitask · **768차원** |
| 검색 | manual k=3 + law k=3 (분리) |
| 규칙 기반 평가 | **19/20 통과** |
| RAGAS | faithfulness **0.699** · recall **0.750** · precision **0.625** |
