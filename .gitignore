__pycache__/
*.pyc
~$*
.DS_Store
.ipynb_checkpoints/

# ── 접속 정보 — 절대 커밋 금지 (2026-08-18 Supabase 이관) ──
.env
.env.*
!.env.example
.streamlit/secrets.toml

# 재생성 가능한 로컬 벡터 DB(용량 크고 바이너리라 git에 안 올림) — 각자
# `python demo/rag_yujin/_4_database.py`로 다시 만들면 됨.
demo/rag_yujin/data/chroma_db/
# ── 재생성물 — run_train.py / run_serve.py 가 다시 만든다 ──
*.db
*.parquet
*.pkl

# ── 재생성물 — 커밋하지 않는다 (2026-08-23) ──
demo/data/*.json
demo/data/*.csv
!demo/data/geocode_cache.json
out_agent_capture.json
에이전트_출력_예시.html
