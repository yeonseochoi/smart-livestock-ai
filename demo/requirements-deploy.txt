# 배포 전용 최소 의존성 — Streamlit Cloud / Hugging Face Spaces 용.
#
# requirements-d.txt(로컬 개발용)와의 차이
#   빠진 것   torch(437MB) · transformers(78MB) · sentence-transformers ·
#             chromadb(66MB) · langchain-community · PyMuPDF · ragas ·
#             xgboost(103MB) · scipy(131MB) · scikit-learn · shap · matplotlib
#             + ko-sroberta 모델 파일 422MB
#   합계      약 1.7GB  ->  약 283MB
#
# 왜 임베딩 모델이 필요 없나
#   대시보드의 RAG 질의는 작업유형 5종에서 자동 생성된다
#   (work_guide.py: f"{work_type} 작업 전후 관리 기준").
#   그 5개의 질의 벡터를 rag.query_cache 에 미리 넣어 두었으므로,
#   배포 서버는 벡터를 만들 필요 없이 pgvector 검색만 하면 된다.
#   적재는 개발자 PC 나 GitHub Actions 에서 _4b_migrate_to_pg.py 로 한다.
#
#   ⚠️ 자유 질문 기능(text_input)을 추가하면 이 전제가 깨진다.
#      캐시에 없는 질의가 오면 임베딩 모델을 로드하려다 실패하므로,
#      그때는 requirements-d.txt 로 배포하거나 임베딩을 API 로 바꿔야 한다.
#
# 왜 ML 라이브러리가 필요 없나
#   대시보드는 모델을 돌리지 않는다. B 가 미리 계산해 Supabase risk_hourly 에
#   넣어 둔 위험도를 읽기만 한다. 학습·서빙은 GitHub Actions 가 담당한다.
#
# 검증 (2026-08-23)
#   위 패키지를 전부 import 차단한 상태에서 Streamlit AppTest 로 전 구간 실행 —
#   7일 캘린더 101행 · 추천 Top3 · 회피 Top3 · RAG 근거 6건 모두 정상.

streamlit>=1.51,<2
pandas==2.3.0
pydeck>=0.9,<1

# Supabase PostgreSQL + pgvector. 벡터는 문자열로 주고받으므로
# pgvector 파이썬 패키지는 필요 없다 (pgvector_store.py 참조).
psycopg[binary]==3.3.4

# Gemini 설명 생성. 키가 없으면 규칙 기반 설명으로 자동 대체된다.
langchain-core==1.5.5
langchain-google-genai==4.3.4
